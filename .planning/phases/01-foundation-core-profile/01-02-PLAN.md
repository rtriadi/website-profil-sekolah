---
phase: "01"
plan: "01-02"
type: "execute"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/(public)/profile/page.tsx
  - src/app/(public)/profile/loading.tsx
  - src/components/profile/identity-section.tsx
  - src/components/profile/narrative-section.tsx
  - src/lib/content/profile-service.ts
autonomous: true
requirements_addressed:
  - PROF-01
  - PROF-02
  - UX-01
---

<objective>
Deliver the public core profile experience so visitors can view school identity, history, vision, and mission on responsive pages.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - src/lib/content/schema.ts
      - .planning/REQUIREMENTS.md
      - .planning/ROADMAP.md
    </read_first>
    <action>
      Implement `src/lib/content/profile-service.ts` to provide typed profile/narrative read APIs for public routes, using temporary in-memory seed source if persistence is not yet wired.
    </action>
    <acceptance_criteria>
      - profile-service exports typed read functions for identity and narrative payloads
      - Returned data covers all fields needed for PROF-01 and PROF-02 rendering
      - Service API can be consumed from server components without client-side fetch requirement
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/app/layout.tsx
      - src/lib/content/profile-service.ts
    </read_first>
    <action>
      Build `src/app/(public)/profile/page.tsx` server-rendered route that composes identity and narrative sections and keeps information hierarchy clear on mobile and desktop.
    </action>
    <acceptance_criteria>
      - `/profile` route renders identity, history, vision, and mission sections
      - Route works as server-rendered page and does not require client data bootstrap
      - Responsive layout is readable at mobile widths (typography and spacing not collapsed)
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <read_first>
      - src/app/(public)/profile/page.tsx
      - src/components/profile/identity-section.tsx
      - src/components/profile/narrative-section.tsx
    </read_first>
    <action>
      Add reusable presentational components for identity and narrative blocks plus loading state placeholder to stabilize perceived performance and layout.
    </action>
    <acceptance_criteria>
      - Identity and narrative section components exist and are used by profile page
      - Loading UI does not shift major layout regions unexpectedly
      - Components avoid hardcoded one-off styling that blocks reuse in later pages
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- Manual route check confirms `/profile` shows identity + narrative content responsively
</verification>

<success_criteria>
- Visitors can see required school profile information end-to-end (PROF-01, PROF-02)
- Responsive quality baseline meets roadmap criterion for mobile usability
- Public profile route is structured for extension in subsequent phases
</success_criteria>
