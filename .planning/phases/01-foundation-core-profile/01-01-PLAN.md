---
phase: "01"
plan: "01-01"
type: "execute"
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/lib/content/schema.ts
  - src/lib/auth/config.ts
autonomous: true
requirements_addressed:
  - ADMN-01
  - UX-01
---

<objective>
Create the phase foundation: project structure, core shared configuration, and baseline contracts for content/auth that unblock profile pages and admin sign-in shell.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - .planning/PROJECT.md
      - .planning/REQUIREMENTS.md
      - .planning/ROADMAP.md
    </read_first>
    <action>
      Initialize runtime and dependency baseline for a Next.js 16 + TypeScript app. Ensure package scripts include dev/build/start/lint/typecheck and establish a `src/`-based structure with app router conventions.
    </action>
    <acceptance_criteria>
      - package.json exists and contains scripts: dev, build, start, lint, typecheck
      - App compiles with a root layout and homepage route present
      - Source tree uses `src/app` and `src/lib` directories
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/lib/content/schema.ts
      - .planning/phases/01-foundation-core-profile/01-RESEARCH.md
    </read_first>
    <action>
      Define minimal phase-1 data contracts in `src/lib/content/schema.ts` for `schoolProfile` and `schoolNarrative` with typed fields covering identity/contact/accreditation/history/vision/mission.
    </action>
    <acceptance_criteria>
      - `src/lib/content/schema.ts` exports typed structures for schoolProfile and schoolNarrative
      - Fields required by PROF-01 and PROF-02 are represented explicitly
      - No phase-2/3 entities (announcements/media CRUD) are introduced in this file
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <read_first>
      - src/lib/auth/config.ts
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Create authentication configuration baseline in `src/lib/auth/config.ts` with staff-role assumptions and protected-route helper primitives for `/admin` namespace checks.
    </action>
    <acceptance_criteria>
      - `src/lib/auth/config.ts` exists and exports auth config + admin guard helper(s)
      - Guard helper can be called by route/page layer without direct DB implementation coupling
      - Config enforces staff/admin identity concept aligned with ADMN-01
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- No missing imports from newly introduced foundation modules
</verification>

<success_criteria>
- Foundation artifacts exist and are reusable by downstream plans
- Data/auth contracts are present for phase 1 scope without over-scoping
- Project can compile cleanly with baseline structure in place
</success_criteria>
