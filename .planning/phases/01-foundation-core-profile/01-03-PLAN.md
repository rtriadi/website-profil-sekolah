---
phase: "01"
plan: "01-03"
type: "execute"
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/app/(admin)/admin/layout.tsx
  - src/app/(admin)/admin/login/page.tsx
  - src/app/(admin)/admin/page.tsx
  - src/lib/auth/session.ts
  - middleware.ts
autonomous: true
requirements_addressed:
  - ADMN-01
  - UX-01
---

<objective>
Establish admin sign-in baseline and protected admin shell so authorized staff users can access admin area while public users are blocked.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - src/lib/auth/config.ts
      - src/lib/auth/session.ts
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Implement `src/lib/auth/session.ts` with staff session read/write helpers compatible with the auth config contract and route-level guard checks.
    </action>
    <acceptance_criteria>
      - session module exports functions to create/read/clear admin session state
      - Session representation includes role/identity attributes required for admin guard decisions
      - Session utilities can be consumed by middleware and admin pages without cyclic dependencies
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/lib/auth/config.ts
      - src/lib/auth/session.ts
      - middleware.ts
    </read_first>
    <action>
      Add protected-route behavior in `middleware.ts` for `/admin` namespace: unauthenticated users redirected to `/admin/login`, authenticated staff allowed through.
    </action>
    <acceptance_criteria>
      - Requests to `/admin` without session redirect to `/admin/login`
      - Requests to `/admin` with valid staff session are allowed
      - Public routes remain unaffected by admin middleware checks
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <read_first>
      - src/app/(admin)/admin/login/page.tsx
      - src/app/(admin)/admin/layout.tsx
      - src/app/(admin)/admin/page.tsx
    </read_first>
    <action>
      Implement admin login page and minimal admin dashboard shell with clear sign-in flow and sign-out affordance for staff users.
    </action>
    <acceptance_criteria>
      - `/admin/login` provides sign-in form path for staff credentials
      - Successful sign-in reaches `/admin` shell; sign-out returns to login state
      - Admin shell uses responsive layout baseline consistent with project UX constraints
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- Manual auth flow check: unauthenticated access redirected, authenticated staff can open `/admin`
</verification>

<success_criteria>
- Admin sign-in baseline is functional for authorized staff users
- Route protection behavior is enforceable and observable
- Admin shell exists as a stable base for phase-3 content operations
</success_criteria>
