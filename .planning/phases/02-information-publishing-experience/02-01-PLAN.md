---
phase: "02"
plan: "02-01"
type: "execute"
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/lib/content/schema.ts
  - src/lib/content/programs-service.ts
  - src/app/(public)/programs/page.tsx
  - src/components/programs/program-list.tsx
  - src/components/programs/facility-list.tsx
autonomous: true
requirements_addressed:
  - PROF-03
---

<objective>
Deliver all Phase 2 schema types, and programs/majors and facilities info — visitor can view school's academic offerings and infrastructure.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - src/lib/content/schema.ts
      - .planning/REQUIREMENTS.md
    </read_first>
    <action>
      Extend `src/lib/content/schema.ts` with ALL Phase 2 entity types + seed data:

      **SchoolProgram** (name, description, slug, type, icon) + `defaultPrograms` (3-4 entries)
      **Facility** (name, description, slug, category) + `defaultFacilities` (3-4 entries)
      **Announcement** (title, slug, summary, content, publishedAt, author) + `defaultAnnouncements` (4+ entries with varied dates)
    </action>
    <acceptance_criteria>
      - schema.ts exports SchoolProgram, Facility, Announcement interfaces
      - schema.ts exports defaultPrograms, defaultFacilities, defaultAnnouncements arrays
      - SchoolProgram includes: name, description, slug, type (IPA/IPS/Bahasa/Umum/etc), icon
      - Facility includes: name, description, slug, category (Lab/Perpustakaan/Olahraga/etc)
      - Announcement includes: title, slug, summary, content, publishedAt (ISO date), author
      - defaultAnnouncements has varied dates for recency sorting verification
      - No phase-3 entities introduced
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/lib/content/schema.ts
      - src/lib/content/profile-service.ts
    </read_first>
    <action>
      Create `src/lib/content/programs-service.ts` exporting async `getPrograms()` and `getFacilities()` typed read functions backed by seed data, matching the Phase 1 service pattern.
    </action>
    <acceptance_criteria>
      - Exports `getPrograms(): Promise<SchoolProgram[]>` and `getFacilities(): Promise<Facility[]>`
      - Functions are async, return seed data arrays
      - No client-side fetch — consumable from server components
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <read_first>
      - src/app/(public)/profile/page.tsx
      - src/components/profile/identity-section.tsx
    </read_first>
    <action>
      Build `src/app/(public)/programs/page.tsx` server-rendered route displaying programs/majors and facilities in sections, plus `src/components/programs/program-list.tsx` and `src/components/programs/facility-list.tsx` presentational components.
    </action>
    <acceptance_criteria>
      - `/programs` renders program majors section + facilities section
      - Server-rendered (no client data bootstrap)
      - Responsive layout matches Phase 1 patterns (max-w-4xl, px-4, py-12)
      - Components accept typed props and are reusable
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- Manual route check confirms `/programs` shows program + facility content
</verification>

<success_criteria>
- Visitors can see school programs/majors and facilities information (PROF-03)
- Structure follows existing Phase 1 patterns
- Route is extendable for detail pages in later phases
</success_criteria>
