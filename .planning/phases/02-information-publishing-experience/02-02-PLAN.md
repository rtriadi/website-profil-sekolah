---
phase: "02"
plan: "02-02"
type: "execute"
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/lib/content/announcement-service.ts
  - src/app/(public)/announcements/page.tsx
  - src/components/announcements/announcement-list.tsx
autonomous: true
requirements_addressed:
  - CONT-01
---

<objective>
Build the announcements list page so visitors can browse published school announcements sorted by recency.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - src/lib/content/announcement-service.ts
      - src/lib/content/profile-service.ts
    </read_first>
    <action>
      Create `src/lib/content/announcement-service.ts` exporting async `getAnnouncements()` (returns all sorted by publishedAt descending) and `getAnnouncementBySlug(slug: string)` functions backed by seed data from schema.ts.
    </action>
    <acceptance_criteria>
      - Imports `defaultAnnouncements` from schema.ts
      - `getAnnouncements()` returns announcements sorted by publishedAt descending (newest first)
      - `getAnnouncementBySlug(slug)` returns single announcement or null if not found
      - Functions are async and consumable from server components
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/app/(public)/profile/page.tsx
      - src/components/profile/narrative-section.tsx
    </read_first>
    <action>
      Build `src/app/(public)/announcements/page.tsx` server-rendered route listing announcements sorted by recency with title, date, and summary visible, plus `src/components/announcements/announcement-list.tsx` presentational component.
    </action>
    <acceptance_criteria>
      - `/announcements` route renders announcement list sorted by date descending
      - Each item shows title, published date, and summary
      - Route is server-rendered static
      - Responsive layout matches existing patterns
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0
- Manual route check confirms `/announcements` shows sorted list
</verification>

<success_criteria>
- Visitors can browse published announcements sorted by recency (CONT-01)
- Announcements are clearly readable with date and title hierarchy
- List page is static-rendered for SEO and fast loads
</success_criteria>
