---
phase: "02"
plan: "02-03"
type: "execute"
wave: 2
depends_on:
  - "02-02"
files_modified:
  - src/app/(public)/announcements/[slug]/page.tsx
  - src/components/announcements/announcement-detail.tsx
  - src/app/page.tsx
  - src/app/layout.tsx
  - src/app/globals.css
autonomous: true
requirements_addressed:
  - CONT-02
  - UX-02
---

<objective>
Deliver announcement detail pages and improve homepage and core layout for stable content hierarchy.
</objective>

<tasks>
  <task id="1" type="execute">
    <read_first>
      - src/lib/content/announcement-service.ts
      - src/app/(public)/announcements/page.tsx
    </read_first>
    <action>
      Create `src/app/(public)/announcements/[slug]/page.tsx` dynamic route that loads announcement by slug and renders full content, plus `src/components/announcements/announcement-detail.tsx` presentational component with title, date, author, and body content.
      Use `generateStaticParams` to pre-render from seed data.
    </action>
    <acceptance_criteria>
      - `/announcements/:slug` renders full announcement content
      - `generateStaticParams` pre-renders all seed announcements
      - 404 for unknown slugs (notFound())
      - Component shows title, date, author, and body content
      - Responsive layout consistent with other pages
    </acceptance_criteria>
  </task>

  <task id="2" type="execute">
    <read_first>
      - src/app/page.tsx
      - src/app/layout.tsx
      - src/app/globals.css
    </read_first>
    <action>
      Improve homepage (`/`) with a hero section mentioning school name, navigation links to /profile, /programs, /announcements, and a "Latest Announcements" section showing the 3 most recent items. Enhance root layout with a header navigation bar and footer.
    </action>
    <acceptance_criteria>
      - Homepage shows school name, tagline, and nav links to profile/programs/announcements
      - Homepage shows 3 latest announcements with links to detail
      - Root layout has a header with navigation and a footer with basic school info
      - Navigation is responsive (collapsed on mobile, horizontal on desktop)
      - Consistent spacing and typography across all public pages
    </acceptance_criteria>
  </task>

  <task id="3" type="execute">
    <read_first>
      - src/app/globals.css
      - src/app/layout.tsx
    </read_first>
    <action>
      Add global CSS variables for consistent typography scale and spacing in globals.css. Ensure all public pages share stable layout (consistent max-width, padding values, heading hierarchy).
    </action>
    <acceptance_criteria>
      - globals.css defines font-size scale and spacing tokens via CSS custom properties
      - All public routes use consistent max-w and padding
      - No layout shift on page navigation
      - `npm run build` exits 0
    </acceptance_criteria>
  </task>
</tasks>

<verification>
- `npm run typecheck` exits 0
- `npm run build` exits 0 (pre-renders announcement detail pages)
- Manual check: homepage shows nav + latest announcements, detail pages render
</verification>

<success_criteria>
- Visitors can read full announcement content (CONT-02)
- Core pages have stable layout and readable content hierarchy (UX-02)
- Homepage serves as effective landing with navigation and latest updates
- Root layout provides consistent header/footer across all pages
</success_criteria>
